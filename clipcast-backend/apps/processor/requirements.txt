# ── PyTorch (cu121) ────────────────────────────────────────────────────────────
torch==2.2.2
torchaudio==2.2.2
torchvision==0.17.2

# ── Core ML / Audio ────────────────────────────────────────────────────────────
numpy>=1.26.0,<2.0
scipy>=1.11.0
scikit-learn>=1.3.0
matplotlib>=3.7.0

# ── Video / Image ──────────────────────────────────────────────────────────────
opencv-python>=4.8.0
ffmpegcv>=0.3.0
scenedetect==0.5.6.1
# Font metrics (word pixel widths) for manually laying out caption lines so
# the per-word background-highlight box can be positioned precisely.
Pillow>=10.0.0

# ── Google AI / Gemini ─────────────────────────────────────────────────────────
google-genai>=2.0.0

# ── AWS / S3 ───────────────────────────────────────────────────────────────────
boto3>=1.34.0

# ── Web framework ──────────────────────────────────────────────────────────────
fastapi[standard]>=0.110.0

# ── Modal ──────────────────────────────────────────────────────────────────────
modal>=0.64.0

# ── Subtitle / Font ────────────────────────────────────────────────────────────
pysubs2>=1.6.0

# ── ASD / TalkNet deps ─────────────────────────────────────────────────────────
python_speech_features
gdown>=4.7.0

# ── ML framework helpers ───────────────────────────────────────────────────────
transformers==4.39.3
accelerate>=0.25.0
datasets>=2.16.0

# ── HF fallback LLM (Qwen2.5-7B-Instruct, used only if Gemini fails) ──────────
# Loaded plain (bf16) through the transformers pin already required by
# WhisperX below, no extra package needed: transformers==4.39.3 already ships
# Qwen2ForCausalLM support (added 4.37), and autoawq/vllm would each demand
# their own newer torch/transformers pins that fight WhisperX's in this same
# image.
huggingface-hub>=0.22.0

# ── Utility ────────────────────────────────────────────────────────────────────
tqdm>=4.66.0
pandas>=2.0.0
python-dotenv>=1.0.0
